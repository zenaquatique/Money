node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Runtime state, not source — see RUSH_ROTATION_STATE_FILE in
# server/render-server.js.
server/.rush-rotation-state.json

# Materialized voiceover files (decoded from base64 Data URIs before
# rendering, deleted again once each job's retention window ends) — see
# VOICEOVER_TMP_DIR in server/render-server.js. Runtime data, not source.
public/tmp-voiceovers/
